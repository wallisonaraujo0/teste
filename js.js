let a = "20";
let b = "10";
let c = parseInt(a) + parseInt(b);
let d = 10;
let e = 20;
let f = d.toString() + e.toString();
const g = {
    name:'wallison',
    lastName:'araujo',
    age:29
}

console.log(`transform sttring in number, the sum of the numbers is  ${c} typeof ${typeof(c)}`);
console .log(`transform number in string, the concatenation of the strings is ${f} tipeof ${typeof(f)}`)

